const name= "Divyansh"
const repoCount= 50
const clgname= "AITM"

console.log(`Hello my name is ${name} and my repocount is ${repoCount} currently studying in ${clgname}`);
const newstring = clgname.substring(0,2)
console.log(newstring)